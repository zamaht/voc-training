{
  "name": "@packages/server",
  "dependencies": {
    "@trpc/server": "^11.3.1",
    "cors": "^2.8.5",
    "openai": "^5.3.0"
  },
  "scripts": {
    "build:dev": "pnpm exec esbuild src/main.ts --bundle --platform=node --outdir=./out/dev/ --sourcemap",
    "dev": "pnpm build:dev --watch",
    "start": "pnpm exec node ./out/dev/main.js"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "esbuild": "^0.25.9",
    "@types/node": "^24.2.1"
  }
}
